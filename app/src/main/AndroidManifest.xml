<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BPT"
        tools:targetApi="31">

        <!-- Activities -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity"> </activity>

        <activity android:name=".LoginActivity"> </activity>

        <activity android:name=".DashboardActivity"> </activity>

        <activity android:name=".AccountActivity"> </activity>

        <activity android:name=".ManualRideAddingActivity"> </activity>

        <activity android:name=".PartsaddingActivity"> </activity>

        <activity android:name=".BikeAddingActivity"> </activity>

        <activity android:name=".BikeDetailsActivity"> </activity>

        <activity android:name=".PartDetailsActivity"> </activity>

        <activity android:name=".ServiceIntervalAddingActivity"> </activity>

        <activity android:name=".ServiceAddingActivity"> </activity>

        <activity android:name=".EditServiceIntervalActivity"> </activity>
    </application>

</manifest>